# Test users exercising each authentication path the driver supports.
# TYPE  DATABASE  USER           ADDRESS  METHOD
local   all       all                     trust
host    all       koja_trust     all      trust
host    all       koja_password  all      password
host    all       koja_scram     all      scram-sha-256
host    all       postgres       all      trust
